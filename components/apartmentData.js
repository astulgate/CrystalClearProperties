import Apartment from "./Apartment.js";

const apt1 = new Apartment(1, 3, 1, 700, 900, false);

const apt2 = new Apartment(2, 3, 1, 700, 850, false);

const apt3 = new Apartment(3, 3, 1, 700, 1400, true);

const apartmentDataArray = [apt1, apt2, apt3];

export default apartmentDataArray;
